module.exports = {

    // cookie serect key
    COOKIE_KEYS: ['super-project-key'],

    // 系统服务启动端口
    SERVER_PORT: process.env.SERVER_PORT || '3000',

    // 默认域名 TODO:删除
    DEFAULT_DOMAIN: process.env.DEFAULT_DOMAIN || 'localhost'
}